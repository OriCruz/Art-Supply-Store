const React = require('react');
const DefaultLayout = require('./Default');

class Index extends React.Component{
    render(){
        return(
            <DefaultLayout>
                
            </DefaultLayout>
        )
    }
}
